name: assessment
version: "1.0.0"
config-version: 2
profile: assessment

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]

target-path: target
clean-targets: ["target", "dbt_packages"]

models:
  assessment:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
